---
  - name: Play for Install Nginx on WebServers
    hosts: all
    become: yes
    become_user: root
    ignore_errors: yes
    serial: 1 #Executes the playbook one host at a time.
    tasks:
       - name: Run Apt Update
         shell: whoami && ls -al /etc/ansible/roles
         tags:
           - update
